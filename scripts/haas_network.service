[Unit]
Description=HaaS Network Server 
After=network.target
After=postgresql

[Service]
User=haas_user
Group=haas_user
WorkingDirectory=/var/lib/haas/
ExecStart=/usr/bin/haas serve_networks
Type=simple
ExecReload=/bin/kill -HUP $MAINPID
Restart=on-failure
RestartSec=5s

[Install]
WantedBy=multi-user.target

